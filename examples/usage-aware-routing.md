synthetic: true
simulation: true

# Usage-aware routing

```text
resource state: PRESERVE
→ triage
→ cache or deterministic path
→ smallest capable team
→ approval if needed
```

The mode is policy input, not a scraped meter. Unknown usage is not treated
as unlimited or zero.
