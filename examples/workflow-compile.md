synthetic: true
simulation: true

# Workflow compile

```text
three verified similar runs
→ compare ordered steps and inputs
→ candidate workflow
→ human review
→ reusable workflow
```

A role definition alone is not a workflow. Evidence, preconditions,
verification, and an approval boundary are required.
