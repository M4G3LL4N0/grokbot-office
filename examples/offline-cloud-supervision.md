synthetic: true
simulation: true

# Offline and cloud supervision

```text
local unavailable
→ request policy check
→ approved cloud worker path
→ bounded task
→ evidence result
→ local state update
```

The cloud step is illustrative. This repository does not claim a live cloud
worker, authenticated account, or spend.
